name: Monitor odds and settle ledgers

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/monitor.yml"
  schedule:
    - cron: "*/10 * * * *"

permissions:
  actions: write
  contents: write

concurrency:
  group: quantbet-ledger
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    env:
      API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - uses: actions/cache@v4
        with:
          path: .cache/api
          key: api-cache-${{ runner.os }}-${{ github.run_id }}
          restore-keys: api-cache-${{ runner.os }}-
      - run: pip install -r requirements.txt
      - run: python main.py monitor
      - run: python tools/settle_alerts.py
      - name: Check settlement health
        id: settlement-health
        continue-on-error: true
        run: python tools/settlement_health.py --max-age-minutes 30
      - name: Build public strong-signal dataset
        run: python tools/build_public_strong_signals.py
      - name: Commit ledger, health report, strong signals and raw API archive
        if: always()
        run: |
          set -euo pipefail
          if git diff --quiet -- bets.json predictions.json intraday_alerts.json strong_signals.json settlement_health.json && test -z "$(git status --porcelain -- data/raw_api)"; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add bets.json predictions.json intraday_alerts.json strong_signals.json settlement_health.json data/raw_api
          git commit -m "Settle QuantBet ledger and archive API data"
          for attempt in 1 2 3; do
            if git push; then exit 0; fi
            git fetch origin main
            git rebase origin/main || { git rebase --abort; sleep 2; }
          done
          echo "Unable to push after retries" >&2
          exit 1
      - name: Fail when settlement is stale
        if: steps.settlement-health.outcome == 'failure'
        run: exit 1
